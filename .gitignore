# Gradle
.gradle/
# (per-module build/ dirs are already ignored by the `build` entry under Typescript)

# Maven (legacy — pom.xml files were removed; kept in case tooling regenerates them)
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties
dependency-reduced-pom.xml

# Eclipse
.project
.classpath
.settings/
bin/

# IntelliJ
.idea
*.ipr
*.iml
*.iws

# NetBeans
nb-configuration.xml

# Visual Studio Code
.factorypath
.vscode

# Typesript
node_modules
build
dist
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.changelog
.npm

# OSX
.DS_Store
*__MACOSX/

# Vim
*.swp
*.swo

# patch
*.orig
*.rej

# Local environment & secrets — real values live only in .env (NEVER commit).
# Keep .env.example (the template) tracked.
.camel-jbang
.env
.env.local
.env.*.local
!.env.example
/.java-version
/keys/*
*.jar
# …but the Gradle wrapper jar MUST be committed so ./gradlew can bootstrap on a fresh checkout.
!gradle/wrapper/gradle-wrapper.jar

**/*.tgz
**/*.vsix
**/*.bak

.quinoa/

/karavan-core/lib/

/karavan-designer/public/kamelets/*
/karavan-designer/public/components/*
/karavan-designer/.firebase/
/karavan-designer/.firebaserc
/karavan-designer/firebase.json

/karavan-app/.quinoa/
/karavan-app/karavan-data*
/karavan-app/src/main/webapp/src/designer/*
/karavan-app/src/main/resources/kamelets/*
karavan-app/docker/secrets/*
karavan-app/docker/data/*
karavan-app/cache-data

karavan-cloud/karavan-secret-real.yaml

docs/install/karavan-docker/data/*

**/dependency-reduced-pom.xml

stats.html

.claude/
CLAUDE.md
# Account-specific deploy artifacts (AWS account id / cluster ARNs / hostnames).
# Keep deploy/helm/karavan/values-eks.example.yaml as the committed template.
deploy/eks/
deploy/helm/karavan/values-eks.yaml
deploy/helm/karavan/values-auth0.yaml

# local debug log
logs.txt

# agentic
ai_docs/postgres-data/
# local dev database (deploy/compose.yaml bind mount)
deploy/postgres-data/

github-creds.json